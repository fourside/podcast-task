{
  "name": "podcast-task",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "check": "biome ci src",
    "deploy:schema": "wrangler d1 execute podcast-task --file=./schema.sql",
    "prepare": "git config --local core.hooksPath .githooks"
  },
  "devDependencies": {
    "@biomejs/biome": "1.5.2",
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.0.4",
    "wrangler": "^3.0.0"
  },
  "dependencies": {
    "@hono/sentry": "^1.0.0",
    "drizzle-orm": "^0.29.3",
    "hono": "^3.12.6",
    "superstruct": "^1.0.3"
  }
}
